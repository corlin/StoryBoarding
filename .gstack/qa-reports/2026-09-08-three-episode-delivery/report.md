# 《第二把钥匙》三集真实链路交付验收

验收日期：2026-09-09

部署站点：https://storyboarding.caifu.social/

工程：https://storyboarding.caifu.social/workspace?id=0d99fe69-5d66-4e51-95d1-9e8beda4e83f
工程 ID：`0d99fe69-5d66-4e51-95d1-9e8beda4e83f`

## 交付结论

已建立内容连续的三集工程，每集 8 镜、每镜 6 秒、每集 48 秒，全片 144 秒。已完成一次真实 MiniMax 视频任务的提交、轮询、R2 回收和采用；为 19 条对白生成初版配音并回收到 R2；另完成一次 OpenRouter 中文 TTS 的真实生成、R2 回收、试听和采用；完成三集独立 MP4、三集完整 MP4、外置 SRT 和 MP4 内嵌字幕轨，并把 `v1.1-subtitled-delivery` 登记为当前剪辑版本。

这是用于验证生产链路的技术样片。EP01 SHOT01 是真实 AI 视频，其他 23 镜使用标明 `TECHNICAL ANIMATIC` 的技术卡，不代表完整视觉成片。

## 内容连续性

| 集 | 钩子 | 集尾断点 | 时长 |
| --- | --- | --- | ---: |
| EP01《钥匙打不开》 | 父亲遗留的缺口黄铜钥匙打不开旧柜门 | 周明说出缺口方向，暴露知情 | 48 秒 |
| EP02《谁换了锁》 | 紧接追问，周明承认换锁 | 钥匙打开铁盒，出现写给林夏的密封信 | 48 秒 |
| EP03《留给谁的信》 | 信封仍密封，周明说明父亲托付 | 信中留下照相馆与欠款，林夏要求看账本 | 48 秒 |

线上浏览器复验看到“全剧目录（3 集）”，三集均为 8 镜；EP02 钩子为“紧接上一集，林夏逼问缺口方向”，断点为“黄铜钥匙打开铁盒，里面出现一封写给林夏的密封信”。

## 真实视频任务与 R2 采用

- 平台任务：`974cb054-7d04-4370-887a-23ed615964a9`
- MiniMax 外部任务：`439617556427046`
- 模型：`MiniMax-Hailuo-02`
- 目标镜头：EP01 SHOT01，镜头 ID `6698c3ec-b508-420c-b8a0-8f278aed5bf0`
- Take：`a753b29b-3f69-45bf-90ef-38a6df68d7d8`
- 状态：真实任务成功，R2 回收成功，采用成功
- R2 视频：`/api/assets/takes/6698c3ec-b508-420c-b8a0-8f278aed5bf0/a753b29b-3f69-45bf-90ef-38a6df68d7d8.mp4`
- 原始媒体：H.264，1366×768，5.875 秒，382,586 bytes，无音轨

项目目标是 9:16，但 MiniMax 文生视频返回 16:9。本次技术样片采用 720×1280 信箱式排版保留完整画面。若要原生竖屏，下一轮应以 9:16 首帧走图生视频链路。

部署后的生成保护已复验：对没有首帧的 EP01 SHOT02 直接请求视频生成，接口返回 HTTP 409 和 `VERTICAL_FIRST_FRAME_REQUIRED`，并要求先生成或上传 9:16 分镜图；请求前后成本接口均为 `total_jobs=1`、`total_takes=20`，证明拦截发生在供应商任务创建和扣费之前。前端在镜头有分镜图时改走图生视频，并把该图作为首帧画幅约束；没有首帧的 9:16 镜头只有在用户明确接受横屏风险后才能走文生视频。此轮未提交新的付费图生视频任务，因此原生竖屏成片质量仍待下一轮低成本实测。

## 配音、字幕和 MP4

- 对白配音：19 条，全部作为音频 Take 上传 R2，并与 DialogueLine 关联。
- 音色：林夏/旁白使用 Tingting，周明使用 Reed。
- 字幕：三集独立 SRT、全片 SRT，以及 MP4 内 `mov_text` 字幕轨。
- 完整带字幕 MP4：H.264 720×1280 + AAC + `mov_text`，144.069660 秒，1,001,754 bytes。
- 剪辑版本：`v1.1-subtitled-delivery`，ID `3f7acae6-cc33-4868-963e-519a8ac561c2`，当前版本；`v1.0-technical-delivery` 已自动取消当前标记。

### OpenRouter TTS 真实验收

- 台词：EP01 SHOT02，林夏“打不开……”；发送给模型的纯台词为 5 个字符，剧本中的角色归属和引号未混入朗读文本。
- 平台任务：`8b1b17fc-2dc9-4953-9e53-7ae0dd28b58b`；状态 `succeeded`。
- Take：`tts-8b1b17fc-2dc9-4953-9e53-7ae0dd28b58b`；状态 `approved`、`is_adopted=1`。
- 模型与音色：`hexgrad/kokoro-82m`、`zf_xiaoxiao`；OpenRouter generation ID：`gen-tts-1788926150-qJK1DWZ94BGdmSfzu3pE`。
- R2：`/api/assets/takes/2ee701a0-51d6-45b3-a1fa-a0eaeca8b700/tts-8b1b17fc-2dc9-4953-9e53-7ae0dd28b58b.mp3`，5,709 bytes，浏览器实播约 1.30 秒。
- 台词关联：DialogueLine `739c5dff-bbe6-4f6c-aec2-16631b49773a` 的 `audio_version` 与 `audio_url` 已更新为该 TTS Take，实际时长已记录为 1.3 秒。
- 版本回切：此前外部上传的 M4A 已取消采用但保留为同台词候选；新旧两条音频均可在台词卡试听和重新采用。
- 媒体传输：R2 资产接口现支持 `Range`，线上 `bytes=0-15` 返回 HTTP 206、`Content-Range: bytes 0-15/5709`，浏览器已成功播放完整 TTS。

现有 `v1.1-subtitled-delivery` MP4 仍包含此前的初版配音；本轮重点验收在线 TTS 生产链路，没有重新合成整片。

四个带字幕 MP4 的本地文件与 R2 文件 SHA-256 完全一致：

| 文件 | SHA-256 |
| --- | --- |
| EP01 | `cfb1832fc41e84d326d6c9bbff3c281f1f0d06d96aeb75ab970e645facea066b` |
| EP02 | `20cb7c456b7798ca38daa06fc3e3b3253eb125b42fb51137d0d74b3927f84566` |
| EP03 | `f807a416d315a1accf97fcbfa1136d8a6a8a13afcdde4c4e893f131215281c13` |
| 三集完整样片 | `ce07f955e9aa828f65ed519e982a69f3430329c05d8c1d37df3960ce86a4e4e6` |

## 线上生产看板证据

部署修复后的浏览器复验显示：共 24 镜、任务 2、视频候选 1、配音 20、视频已采用 1、视频待审 0、视频采用率 100%。EP01 SHOT01 的真实视频可直接预览并标记为“当前采用版本”；其余 23 镜准确显示为待生成，配音 Take 不再污染视频候选和镜头状态。EP01 SHOT02 台词卡显示 2 个配音候选，OpenRouter 版本展示模型、音色、试听、采用/退回和 R2 查看入口，并标记为“已采用”。

展开“导出整集”后，页面可识别当前 `v1.1-subtitled-delivery`，并提供完整 MP4、三集 MP4、三集 SRT、完整 SRT 和交付清单的 R2 下载入口。

成本接口返回 `total_jobs=2`、成功 2、失败 0、`has_unknown_cost=true`、`cost_complete=false`。平台没有从供应商响应取得 MiniMax 或 OpenRouter 的真实扣费，因此没有用估算金额冒充实付成本；应以供应商账户账单为准。

## 验收矩阵

| 项目 | 结果 | 证据与边界 |
| --- | --- | --- |
| A01 真实提交、轮询、回收、刷新后可见 | 通过 | 1 个 MiniMax 任务成功，R2 Take 可访问，线上看板显示已采用 |
| A02 幂等与并发回调 | 机制通过，线上压力未执行 | 视频提交使用 D1 单语句原子预留，完成轮询以任务 ID 生成固定 Take ID；SQLite 语义验证可阻止同镜头第二个活动任务，但未对真实供应商发起付费并发压力测试 |
| A03 多候选采用/退回 | 部分 | 已完成真实视频采用；未额外付费生成第二个视频候选 |
| A04 配音时长与可用性 | 技术通过 | 19 条初版配音均回收并入片；新增 OpenRouter TTS 已真实生成、R2 回收、试听、采用并记录 1.3 秒时长，自然度仍需人工听审 |
| A05 三集连续性 | 内容通过 | 文本、道具状态和钩子连续；视觉连续性只验证 1 个 AI 镜头，其余为技术卡 |
| A06 弱网与中断恢复 | 未执行 | 本轮没有注入弱网或浏览器中断 |
| A07 完整交付 | 通过 | 三集 MP4、完整 MP4、AAC、SRT、内嵌字幕、R2 与 EditVersion 均完成 |
| A08 成本可追溯 | 部分 | 任务数量可追溯，实际供应商扣费未知，平台正确标记成本不完整 |

## 可复现材料

- `artifacts/create-series.json`：三集工程输入，关闭预览图自动生成以避免额外付费。
- `artifacts/generate-delivery.js`：通过 `REAL_VIDEO=/path/to/recovered.mp4 node artifacts/generate-delivery.js` 替换 EP01 SHOT01，生成三集/全片 MP4、SRT 与内嵌字幕轨。
- `artifacts/render-cards.py`：生成明确标识的 9:16 技术卡。
- `artifacts/delivery/delivery-manifest.json`：交付清单。

## 后续产品优化优先级

1. 接入供应商账单或额度变动，记录真实扣费；在此之前持续显示“成本不完整”。
2. 下一轮视觉验收应先生成角色/场景基准图，再按每集 2 个关键镜头扩展，逐步替换技术卡，控制成本并检验跨集一致性；首个新增镜头同时验证原生 9:16 图生视频质量。
3. 从生成音频元数据自动回填真实时长，并增加人工听审结论字段；当前可试听、采用与退回，但时长仍由用户确认录入。
4. 统一工作台快照版本与剪辑交付版本的命名和入口，减少顶部 `v1.0` 与当前交付 `v1.1` 并存造成的理解成本。
5. 在隔离测试账户增加真实并发提交和重复轮询压力验收；MiniMax 当前公开的视频创建接口未提供幂等请求键，连接中断且供应商响应丢失时仍需人工到供应商任务列表核对。

## 2026-09-09 外部优化后闭环复验

本轮基于已部署站点重新核验 Speech 配置、模型试音、看板生成、R2 回收、试听和采用，并修复了外部优化带来的两个生产风险：生成任务曾把未知供应商扣费写成 `USD 0`，Kokoro 中文默认声线曾被替换为英文声线。当前 TTS、图片和视频生成任务均将未取得的真实费用标为 `unknown`，看板显示“费用待账单回填”；Kokoro 默认女声、男声和旁白恢复为 `zf_xiaoxiao`、`zm_yunxi`、`zf_xiaobei`。

- OpenRouter Speech 模型目录在线加载成功，共 18 个模型。免费 `deepgram/flux-tts:free` 试音链路可完成，但目录提供的是英语声线，不适合作为本中文短剧的验收声线。
- 免费 `fish-audio/s2.1-pro-free:free` 试音遭遇 Cloudflare 主机级 HTTP 502；修复后设置页会保留并显示上游 HTML 错误详情，不再只显示无信息的 `HTTP 502`。
- `hexgrad/kokoro-82m` 中文试音成功；付费或未知价格模型在请求前显示模型、字符数、单价和确认框。
- 制作看板此前硬编码英文 Kokoro 声线，现改为实时读取全局 Speech 配置。线上 EP01 SHOT02 的局部覆盖输入为空，候选仅为 `zf_xiaoxiao`、`zm_yunxi`、`zf_xiaobei`，页面明确提示留空时按角色使用设置中的默认声线。
- 修复部署后以空白局部覆盖再次生成林夏“打不开……”，平台任务 `558b9ff1-4abe-4242-b302-2b9e5e777991` 成功，自动选择 `zf_xiaoxiao`，并生成 Take `tts-558b9ff1-4abe-4242-b302-2b9e5e777991`。该 Take 已采用，旧外部 M4A 和较早 TTS 均保留为可回切候选；误用 `af_bella` 的候选保持退回状态。
- 新 R2 音频为 `/api/assets/takes/2ee701a0-51d6-45b3-a1fa-a0eaeca8b700/tts-558b9ff1-4abe-4242-b302-2b9e5e777991.mp3`，5,709 bytes。浏览器媒体状态为 `readyState=4`，时长 1.301542 秒；`Range: bytes=0-63` 返回 HTTP 206、`Content-Type: audio/mpeg` 和 `Content-Range: bytes 0-63/5709`。
- 线上看板最终显示任务 5、视频候选 1、配音 23、视频已采用 1、成功 5/失败 0，并继续标记成本不完整。任务数量和产物可追踪，但真实供应商扣费仍必须等待账单回填。

本轮相关生产提交为 `f1d51b3`（中文声线与真实成本语义）、`a6a2a69`（保留试音错误详情）和 `695cde2`（制作看板继承 Speech 配置）。三次前端部署均成功，`f1d51b3` 的后端 Worker 部署也成功。

# Cloudflare Pages redirect for API routes (Fallback when Functions are bypassing)
/api/*  https://storyboarding-api.caifu.social/api/:splat  307

# Client-side SPA routing fallback for direct URL visits
/workspace/*  /workspace.html  200
